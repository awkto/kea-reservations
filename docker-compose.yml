version: '3.8'

services:
  kea-gui:
    build: .
    container_name: kea-lease-manager
    ports:
      - "5000:5000"
    volumes:
      - ./config:/app/config
    restart: unless-stopped
    networks:
      - kea-network

networks:
  kea-network:
    driver: bridge
